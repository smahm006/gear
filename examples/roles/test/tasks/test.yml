- !shell
  with:
    tags: ['dev', 'go']
    sudo: true
  name: test 1
  cmd: "ls"
  save: go_latest
  and:
    retry: 3

- !shell
  name: test 2
  cmd: "echo hello"
  save: hello
  with:
    tags: ['naa']

- !shell
  name: test 3
  cmd: "test"
  with:
    sudo: true
